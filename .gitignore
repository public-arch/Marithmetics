# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db

# Master suite AoR outputs (do not commit)
audits/_outroot_tmp/

# Generated demo artifacts (do not commit)
demos/**/_artifacts/
demos/**/DEMO*_BUNDLE_*/
demos/**/demo*_cert_bundle_*/

# Release zips (keep ignored generally; allow authority archive if desired)
*.zip
!GUM/authority_archive/*.zip

# AoR run directories are local artifacts; keep only release zips
# Generated reports (keep only LATEST.pdf tracked; ignore timestamped outputs)
gum/reports/GUM_Report_v*.pdf
gum/reports/GUM_Report_v*.pdf.manifest.json
gum/reports/GUM_Report_v*.manifest.json
!gum/reports/LATEST.pdf
!gum/reports/LATEST.pdf.manifest.json
!gum/reports/.gitkeep
!gum/authority_archive/releases/*.zip

# --- build outputs ---
# Never track report PDFs or their manifest JSONs (they change every run)
gum/reports/*.pdf
gum/reports/*.pdf.manifest.json
!gum/reports/README.md

# Never allow uppercase legacy output root

# Local Python environment and caches
.venv/
**/__pycache__/

# Never allow uppercase legacy output root (legacy slop)
GUM/

# Authority-of-Record (AoR) outputs
# We KEEP the canonical AoR folders tracked for stable, citeable URLs.
!gum/authority_archive/**

# Generated reports: keep only LATEST.pdf tracked; ignore timestamped outputs
gum/reports/GUM_Report_v*.pdf
gum/reports/GUM_Report_v*.pdf.manifest.json
!gum/reports/LATEST.pdf
!gum/reports/LATEST.pdf.manifest.json
!gum/reports/README.md

# --- generated / local outputs (never commit) ---
gum/authority_archive/AOR_*/
gum/authority_archive/releases/
gum/reports/*.pdf
gum/reports/*.pdf.manifest.json

# local backups / scratch
*.bak
*.bak_*
*.BAD.*.py
zz_archive/local_scratch/
